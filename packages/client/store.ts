export function frameStore(socket, parser) {
  const frames = [];

  socket.subscribe('__frame', () => {

  });

  function findEntity() {

  }

  function getFrame() {
      
  }

  return {
    getFrame,
    findEntity,
  };
}
